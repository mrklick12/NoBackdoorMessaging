__pycache__/
*.pyc

# per-instance private state (keys, decrypted logs, config) — never commit these
alice_data/
bob_data/
*_data/
keys/
logs/
config.json
*.pem

# relay's public-key store and mailbox (both generated at runtime)
relay/database.db
instance/

.venv/
venv/
